# エージェントの運用・開発・配布の指示

## 1. AIモデルの使い分け

AIモデルを使い分ける場合は、コスト効率を重視する。精度よりコスト削減を優先するのではなく、必要十分なモデルを選ぶ。

- Luna：大量処理、定型処理、軽い実装、一次判定。
- Terra：常駐オーケストレーター（作業全体の司令塔）、タスク分解、進行管理、統合判断。
- Sol：難しい実装、複雑な推論、品質レビュー。
- Astra：最重要判断、大規模設計変更、他モデルで解決できない難問のみ。

原則としてAstraを常時オーケストレーターにはせず、Terraを中心に使う。難易度が上がった場合のみ、Terra → Sol → Astra の順でエスカレーションする。

Astraへ渡す際は、生ログや全コンテキストをそのまま渡さず、下位モデル側で要点・エラー・関連コード・判断候補を圧縮してから渡す。

## 2. 分担と進行管理

- 主担当は、依頼整理、タスク分解、進行管理、通常判断、結果統合を担う。大規模な作業では、独立して進められる調査・実装・レビューを、上記の役割に応じて分担する。
- 小さな作業では、分担に伴う時間とトークン消費も考慮し、必要な範囲だけサブエージェントを使う。モデル切り替え自体を目的にしない。
- サブエージェントへの依頼には、目的、対象範囲、必要な根拠、期待する成果を明記し、主担当が結果を確認して統合する。
- 同じ作業を担当するサブエージェント間の直接のやり取りを許可する。担当範囲内の質問、発見の共有、依存関係の調整、レビュー依頼は、主担当の仲介や利用者への追加確認なしで行ってよい。
- やり取りは必要な相手に要点と根拠を絞って行い、不要な全員送信や反復を避ける。重要な結論、未解決事項、担当範囲の変更が必要な場合は主担当へ共有し、最終的な判断と結果の統合は主担当が担う。
- サブエージェントを起動する場合は、タスク名に作業内容・使用モデル・推論力を含める。ツール上の名前に使えない文字は `_` へ置換する（例: `review_changes_gpt_5_6_sol_high`）。
- 実際に利用できるモデルと切り替え手段を確認し、利用できない場合は制約を正確に伝える。役割の指定だけでモデルを切り替えたと報告しない。

## 3. 作業と対話の基本

- 日本語で結論から分かりやすく説明する。通常は簡潔にし、判断や操作に必要な部分には具体例を添える。専門用語は初出で短く説明し、過剰な称賛や説明の繰り返しを避ける。利用者の考えに問題がある場合は、根拠と代案を示す。
- 相談・比較・方針検討と、実行の依頼を区別する。実行を依頼された場合は、提案だけで終えず、依頼範囲の作業と必要な検証まで進める。
- 軽微で元に戻せる判断は、文脈から合理的に決めて進める。成果を大きく左右する不明点は確認し、回答待ちでも独立して進められる作業は継続する。すでに承認された内容を繰り返し確認しない。
- 実データ・実ファイル・実行結果を優先し、確認済みの事実、推測、未検証、未実装を区別する。確認していないことを完了扱いにしない。変わりやすい情報は、必要に応じて最新の一次情報を確認する。
- 検証は変更の影響と重要度に合わせる。必要な確認を終えたら、理由なく同じ検証を繰り返したり、依頼範囲を広げたりしない。
- 長い作業では節目に短く進捗を伝え、最後に成果、検証結果、残っている課題を簡潔に報告する。途中の追加指示を反映し、目的の変更がなければ元の作業も継続する。
- スキルやAGENTS.mdの記述が原因で停止や追加確認が必要と判断した場合は、該当ファイル、該当記述、適用理由を示す。現在の依頼と過去の指示がどこまで適用されるかを確認する。

## 4. 承認範囲とコミット・プッシュ

- 削除、原本の不可逆な上書き、外部への送信・公開、購入などは、その操作への承認がある場合に実行する。確認が必要な場合でも、承認済みの範囲で調査や下書きを進め、具体的な対象と変更内容を示す。
- コミット・プッシュは明示的な依頼または継続許可がある範囲で行う。このプロジェクトでは、2026-09-05の利用者指示により、検証を終えた区切りごとのコミット・プッシュが継続許可されており、毎回の追加確認は不要。これは作業中のルールであり、時刻指定の定期実行ではない。
- 継続許可の対象は、通常アプリの `https://github.com/graphite1/Digitalbillder_GR.git` と、`C:/Users/rinnt/Documents/Codex/Digitalbuilder＿GR/作業補助/配布サイト` のSites専用リモート。
- コミット前に差分と対象ファイルを確認する。検証した変更だけを意味のある単位で保存し、実データ・秘密情報・作業途中の変更を一括で含めない。並行作業で対象が変更中なら区切りを待つ。変更がなければコミットしない。
- 通常のプッシュが競合・認証エラー等で失敗した場合は履歴を強制上書きせず、原因と未完了の操作を報告する。
- サイトの配布ファイルと変更履歴の一般公開は許可済み。管理操作は所有者限定とする。実請求書、台帳、予算原本、ログイン情報、署名秘密鍵を配布しない。
- 利用者が評価だけ・ローカル試験だけ・コミットや公開の保留を指定した場合は、その最新指示の範囲を守る。

## 5. 作業場所とDigital Billderへのアクセス

- 開発・検証・構想の記録・配布物の作成は開発本体内を拠点に行い、本体と同階層へ補助フォルダーを新設しない。補助作業は本体内の `作業補助/` に集約する。
- 構想と進度は `docs/STATUS_AND_ISSUES.md` に記録する。配置と移動記録は `docs/WORKSPACE_LAYOUT_PLAN.md` を参照する。配布検証の実データやサイト専用リポジトリを通常アプリのGit・配布物へ含めない。
- Digital Billderへの通常の取得速度は維持する。不要な全件再取得・重複アクセス・連続リトライを避ける。更新配布の試験にDigital Billderの再取得を使わない。
- Web書込みの永続凍結を更新だけで解除しない。「次に回す」は実装・実行しない。

## 6. バージョン公開と更新の保護

- 新バージョンの公開が確定したら、同じ作業の中で配布サイトの更新まで行う。確定とは、版番号・配布番号・変更履歴が決まり、対象ソースの検証とコミット・プッシュが完了した状態を指す。番号だけの変更や開発途中のコミットを公開の根拠にしない。
- 公開時は、確定したソースから署名済みアプリ更新とWindows同梱版を作成し、配布サイトのファイル・変更履歴・ダウンロード案内を更新する。サイトからの取得、署名・ハッシュ、アプリ側の更新・起動を確認し、公開した版と検証結果を報告する。
- 公開版は署名付き配布情報・配布履歴と照合し、開発用 `invoice_manager/version.py` の値だけで判断しない。説明書だけの修正では、それだけを理由にアプリの版番号を上げない。
- 公開済みの版・ファイルを差し替えない。同じ版を再署名して置換せず、新しい版で変更履歴を残す。問題のある版は配布停止し、正常版への復帰条件を確認する。
- 台帳の場所とWindows資格情報ストアの識別子を維持する。更新時に開発中のcheckoutや利用者データを上書きしない。初版の更新機構は同じDB形式・同じ実行環境のコード更新に限定する。
- 署名、ファイル整合性、互換性、更新前バックアップ、終了待ち、起動確認、失敗・電源断後の復旧を維持する。

## 7. Windows 11での初回導入

- 初めて使うWindows 11のPC向けには、小さなセットアップEXEでPython・必要な部品を取得する軽量版を優先する。
- 既存のEdgeをheadless（画面を表示しない実行）で再利用し、EdgeがないPCだけ専用ブラウザーを追加取得する。既存同梱ブラウザーを持つ環境はその利用を維持する。
- 導入成功後はセットアップ専用の一時ZIPを削除し、実行に必要なruntime・台帳は残す。起動EXEとユーザー用ショートカットを用意する。
- 開発PCのPython、.venv、PATH、ユーザーのsite-packagesに依存させず、実機確認と未確認範囲を記録する。

配布操作と初回導入の説明: [アプリ更新の運用](docs/UPDATE_GUIDE.md)。
