# 開発・配布の指示

- 2026-09-05の利用者指示により、開発・検証・構想の記録・配布物の作成は開発本体内を拠点に行い、本体と同階層へ補助フォルダーを新設しない。構想と進度は `docs/STATUS_AND_ISSUES.md` に記録する。補助作業は本体内の `作業補助/` に集約済み。配置と移動記録は `docs/WORKSPACE_LAYOUT_PLAN.md` を参照する。配布検証の実データやサイト専用リポジトリを通常アプリのGit・配布物へ含めない。

- 2026-09-05の利用者指示により、今後の開発では変更がまとまり、必要な検証を終えた区切りごとにコミット・プッシュを行う。対象は通常アプリの `https://github.com/graphite1/Digitalbillder_GR.git` と、下記のサイト専用リポジトリ。毎回の追加確認は不要。これは作業中のエージェントが守るルールであり、時刻指定の定期実行ではない。
- コミット前に差分と対象ファイルを確認する。検証した変更だけを意味のある単位で保存し、実データ・秘密情報・作業途中の変更を一括で含めない。並行作業で対象が変更中なら区切りを待つ。変更がなければコミットしない。通常のプッシュが競合・認証エラー等で失敗した場合は履歴を強制上書きせず、原因と未完了の操作を報告する。
- 新バージョンの公開が確定したら、同じ作業の中で配布サイトの更新まで行う。確定とは、版番号・配布番号・変更履歴が決まり、対象ソースの検証とコミット・プッシュが完了した状態を指す。番号だけの変更や開発途中のコミットを公開の根拠にしない。
- 公開時は、確定したソースから署名済みアプリ更新とWindows同梱版を作成し、配布サイトのファイル・変更履歴・ダウンロード案内を更新する。サイトからの取得、署名・ハッシュ、アプリ側の更新・起動を確認し、公開した版と検証結果を報告する。公開版は署名付き配布情報・配布履歴と照合し、開発用 `invoice_manager/version.py` の値だけで判断しない。
- 説明書だけの修正では、それだけを理由にアプリの版番号を上げない。利用者が評価だけ・ローカル試験だけ・コミットや公開の保留を指定した場合は、その最新指示の範囲を守る。
- 公開済みの版・ファイルを差し替えない。同じ版を再署名して置換せず、新しい版で変更履歴を残す。問題のある版は配布停止し、正常版への復帰条件を確認する。
- サイト専用リポジトリへの公開に必要なコミット・プッシュは利用者から許可済み。サイト側の対象は `C:/Users/rinnt/Documents/Codex/Digitalbuilder＿GR/作業補助/配布サイト` とそのSites専用リモート。通常アプリ側は上記の継続許可に従う。
- サイトの配布ファイルと変更履歴の一般公開は許可済み。管理操作は所有者限定。実請求書、台帳、予算原本、ログイン情報、署名秘密鍵を配布しない。
- 台帳の場所とWindows資格情報ストアの識別子を維持する。更新時に開発中のcheckoutや利用者データを上書きしない。初版の更新機構は同じDB形式・同じ実行環境のコード更新に限定する。
- 初めて使うWindows 11のPC向けには、小さなセットアップEXEでPython・必要な部品を取得する軽量版を優先する。既存のEdgeをheadlessで再利用し、EdgeがないPCだけ専用ブラウザーを追加取得する。既存同梱ブラウザーを持つ環境はその利用を維持する。導入成功後はセットアップ専用の一時ZIPを削除し、実行に必要なruntime・台帳は残す。起動EXEとユーザー用ショートカットを用意する。開発PCのPython、.venv、PATH、ユーザーのsite-packagesに依存させず、実機確認と未確認範囲を記録する。
- 署名、ファイル整合性、互換性、更新前バックアップ、終了待ち、起動確認、失敗・電源断後の復旧を維持する。Web書込みの永続凍結を更新だけで解除しない。「次に回す」は実装・実行しない。
- Digital Billderへの通常の取得速度は維持する。不要な全件再取得・重複アクセス・連続リトライを避ける。更新配布の試験にDigital Billderの再取得を使わない。
- サブエージェントを適切に割り振り、軽微な作業はSparkが利用可能なら優先する。利用できない場合はLuna等の軽量モデルを使い、その制約を正確に伝える。

配布操作と初回導入の説明: [アプリ更新の運用](docs/UPDATE_GUIDE.md)。
