# 現状と課題

確認日: 2026-09-05（日本時間）

**新着取込、工種の手動振分、保管済み実績・履歴候補、PDF予算の一括登録、アプリ内更新を実装済みです。コード公開版はv1.0.4（sequence 4）です。** 初回導入用の97 KiBセットアップEXEを公開し、初回取得量を約207 MiBから約85 MiBへ削減しました。v1.0.1・v1.0.2は、旧launcherが`__pycache__`を生成して署名台帳検証に失敗する問題により配布停止しています。`app.py`冒頭の`sys.dont_write_bytecode=True`とlauncherの`-B` / `PYTHONDONTWRITEBYTECODE`で修正しました。Webへの「編集を保存」はまだ無効です。画面変更への耐性を高める通信データ読取りは、通常版から分離した3件の照合試験まで進んでいます。

次の開発は、**既存の取得結果を保ったまま新しい読取り結果を比較する試験モード**が候補です。予算・実績を判断に使う前には、税抜額、誤請求の除外、工種対応の正確さも直す必要があります。今回の実機検証では必要範囲のCSV/ZIPを取得し、隔離台帳へ実請求1件・PDF添付2点を登録しました。通常の主台帳は変更していません。

## 承認フローの進度マップ（構想・2026-09-05利用者確認）

目指す流れは、**アプリで請求確認・工種振分・上位者への提出準備 → 利用者がDigital Billderで最終確認・送信 → 送信完了を確認したデータをアプリ内の原価管理・予算との比較・最終着地予想へ反映**する形です。今回の更新は構想の記録だけであり、送信状態の管理や送信後の計上を実装したものではありません。

| 段階 | 担当・目指す操作 | 現状の進度 | 残る作業・完了条件 |
| --- | --- | --- | --- |
| 1. 請求の取込・確認 | アプリで新着請求と添付PDFを確認する | 実装済み。隔離環境で実請求・PDFの取込と表示を確認 | 日常運用で取込漏れ・重複・除外の扱いを確認する |
| 2. 工種振分・提出準備 | アプリで振分を入力し、金額とWebとの差分を確認する | 手動振分・差分確認は実装済み。Webへの編集保存は無効 | 税抜額の整合を修正する。Web編集保存は編集可能な試験請求で検証してから接続する |
| 3. 上位者へ提出 | 利用者がDigital Billder上で最終確認し、送信する | アプリ外の利用者操作として扱う | アプリは「アクション → 次に回す」を実装・実行しない。提出準備と実際の送信を区別する |
| 4. 送信完了の確認 | 送信した請求と、その時点の金額・振分をアプリ側で確認する | 構想のみ・未実装 | 送信完了を判断する根拠、確認方法、日時・対象明細の記録方法を決める。未確認の請求を送信済みと扱わない |
| 5. 原価・予算・最終着地への反映 | 確認できた送信済みデータをアプリ内の実績と予算比較に反映し、残工事見込と合わせて最終着地を確認する | 保管済み履歴の集計・予算・最終原価見込は実装済み。送信完了を起点とする一連の反映は未実装 | 準備中と送信済みを区別し、修正・取消・再取得で二重計上しない。金額・除外・対応コード・履歴鮮度の課題を解消する |
| 保留. OBIC7への入力・連携 | 将来必要になればアプリの整理結果を参照して入力する | 利用者の最新指定により当面保留 | OBIC7への入力・連携、転記用画面、入力済み管理は今回の開発対象に含めず、再開時に相談する |

### 実装前に決めること

- **送信済み・承認済み・保管済みは同一視しない。** 現在の実績集計は保管済み履歴が対象であり、上位者への送信完了と同じ状態かは未確認。状態の対応関係とアプリ内での計上条件を確認してから接続する。
- 送信完了の確認をWebの読取りで行うか利用者の確認記録で行うかは未決定。今回の構想は自動送信・自動承認・自動検知の実装を意味しない。
- 送信後の修正・取消・差戻しが起きた場合の扱いと、既存の保管済み実績との重複防止を設計する。アプリ内で実績に計上することと、上位者が承認したことを区別する。
- 最終着地予想は、反映した実績だけで自動確定するものではない。現行の残工事見込の入力と、未入力・未確認値を未確定として示す扱いを維持する。
- アプリ内の原価・予算管理は引き続き対象とする。OBIC7関連を保留しても、アプリ内の金額の正確さを後回しにする意味ではない。

## 次に取り組む順番

| 順番 | 課題 | 具体的な作業と完了条件 |
| --- | --- | --- |
| 設計 | 承認フローと計上条件を整理 | 上記の進度マップを基準に、送信完了の根拠、保管済みとの関係、修正・取消時の扱いを決める。構想と実装済み機能を区別し、OBIC7関連は保留する |
| 1 | 画面変更に強い読取りの比較試験 | 通常の画面表示で既に受信した通信データを使い、現在の画面読取りと同じ請求情報・査定明細になるか照合する。試験結果は台帳へ反映しない。複数明細、8%・非課税、保管取消、参照先欠落を検証し、どこまで一致を保証できるか決める |
| 2 | 金額と集計対象の正確さ | 10%逆算表示と実税率の不整合、誤請求が履歴に入る問題、未確認工種を実績0円にする問題を修正する。未確認値で原価見込を確定しない |
| 3 | 履歴更新の使いやすさ | 実績の取得日時、中止、失敗記録を追加する。詳細を読んでいないのに取得日時だけ新しくなる問題を修正し、全件再取得を必要とする場面を減らす |
| 4 | Webへ編集保存 | 編集可能な試験請求が用意できたら、差分確認・構造変更時の永続凍結・保存後照合を実画面へ接続する。「アクション → 次に回す」は実行しない |
| 5 | PDF予算の確認を強化 | 小計と明細の二重計上、原本合計との不一致を検知する。帳票の種類に応じてページ選択・複数ページ・OCRを検討する。手動修正は維持する |
| 接続 | 送信完了後のアプリ内実績反映 | 計上条件の設計と金額・履歴の修正後に、確認済みの送信データを原価・予算比較・最終着地予想へ接続する。準備中との区別、既存履歴との重複防止、修正・取消を検証する。現在は未実装 |
| 将来 | AIによる振分案・MCP接続 | PDFの内容と履歴を根拠に振分案を作り、利用者が修正・採用する。現在は方針の文書化のみで、API接続やAI振分は未実装 |

番号1〜5は既存の技術課題の順番です。承認フローの設計と接続を追加しましたが、今回実装に着手したものではありません。1〜3は編集可能な試験請求を待たずに、ローカルの実装と架空データでの検証を進められます。ただし、保管取消や査定変更が通信データへどう反映されるかなど、実際の状態遷移の確認は未解決です。4は試験請求での確認が必要です。集計を実運用に使う際は2を優先します。

### 取得速度とサーバー負荷の方針

- 利用者の最新指定に従い、**通常の取得速度は維持し、余分な待ち時間は追加しません**。既存の並列取得を一律に1件ずつへ落とす方針は採りません。
- 不要な全件取得、同じ請求の重複取得、失敗後の連続リトライを避け、総アクセス件数を抑えます。
- 比較試験では、同じ表示で受信済みのデータを利用する設計を優先します。比較のために同じ請求を二重に開かないようにします。
- アクセス制限や継続的な通信エラーが発生したら中断します。新しい回数制限・中断制御を実装済みという意味ではなく、今後の試験・実装方針です。

## 現在の通常版

将来方針（未実装）: [MCP接続で追加請求をPDF内部情報・OCRから判断し、根拠付き振分案を利用者の最終確認へ引き継ぐ](MCP_INVOICE_ALLOCATION_PLAN.md)。今回の追記は方針の記録であり、下記の実装状況とは区別する。

更新機構（実装・公開済み）: [アプリ更新の運用](UPDATE_GUIDE.md)。[配布サイト](https://digitalbuilder-gr-updates.rinntyu2000.chatgpt.site)でv1.0.4と変更履歴を公開。サイトを開かず、アプリの「アプリの更新」から取得できます。初めて使うPCには、必要な実行環境を取得するセットアップEXE・起動EXE・ショートカットを用意しました。セットアップ後は一時ZIPを削除し、必要なruntimeと台帳を残します。新しいWindows版（distribution sequence 3、code sequence 4）は既存Edgeを利用し、EdgeがないPCだけ専用ブラウザーを取得します。旧同梱版は引き続き同梱ブラウザーを使います。公開EXEからの導入、資格情報保存、実請求1件・PDF2点の取込と内部表示、再起動、旧版からの更新とバックアップをこのWin11 x64で確認しました。詳細・未検証範囲は[DISTRIBUTION_VERIFICATION.md](DISTRIBUTION_VERIFICATION.md)を参照してください。今後も新しい版番号・配布番号で署名し、公開済みファイルを差し替えず配布します。

| 項目 | 確認結果 |
| --- | --- |
| ローカル請求書 | 81件 |
| 添付ファイル登録 | 115件 |
| 工事 | 4件 |
| 工種マスタ | 40件 |
| ローカル工種振分 | 0件 |
| 保管済み履歴・実績明細 | 履歴81件・査定明細96件。同期成功記録あり |
| 保存済み工事予算・予算行 | ともに0件 |

件数は更新機構の実装前に通常版のSQLiteを読み取り専用で確認した時点の値です。履歴取得は利用者の成功確認も得られています。履歴エラー画像に対する追加修正は行っていません。ローカル振分と保存済み予算が0件のため、これらの本番データでの運用実績はまだありません。

| 機能 | 実装状況 | 残る確認・制約 |
| --- | --- | --- |
| 起動・環境 | 仮想環境と起動.batの修正済み。固定launcherが検証済みの版を選んで起動 | 長期利用時の運用確認 |
| アプリ内更新 | HTTPS取得、署名・ハッシュ・互換性検証、別フォルダ準備、DBバックアップ、起動確認、失敗・電源断時の復旧を実装。作業中の再起動を保留 | 同じPython・依存関係・DB形式のコード更新に限定。DB移行、実行環境、固定launcher自体の更新は別途対応 |
| 配布サイト | 公開ダウンロード・変更履歴と所有者限定の登録・公開・配布停止を実装。v1.0.3とWindows portable r2を公開 | 所有者のブラウザーでの初回ログイン操作は未確認。一時トークンは無効ハッシュ・過去の期限へ置換し、旧トークン403を確認済み |
| 管理画面 | 採用指定されたプレビューを通常版へ反映済み | 改修範囲は管理画面。請求一覧全体の再デザインは行わない |
| 新着請求取込 | ブラウザー画面を出さずCSV/ZIPを取得。初見ID判定、破棄済み除外、個別除外、取込直前の再検証を実装 | 内部ではブラウザーを使用する。選択件数が少なくても添付ZIP全体を取得するため待ち時間が残る。定期実行は未実装 |
| 工種振分 | 任意コードと10%・8%・非課税の行入力、修正に対応 | 請求書本体の税抜表示との整合が未完了 |
| Web差分確認 | 現在の査定内容を読み、ローカル振分との比較が可能 | 実際の「編集を保存」は無効。上司へ送る「次に回す」は対象外 |
| Web構造変更ガード | 許可操作、画面構造比較、永続凍結の基盤と単体テストあり | 実編集画面の承認済み構造が未設定。実画面と保存操作の接続は未実装 |
| 保管済み実績・履歴候補 | 工事×会社×工種の実績集計と利用頻度からの工種候補を実装。履歴取得成功を確認済み | 定期自動検知は未実装。鮮度管理・中止操作に課題あり |
| PDF予算 | 2ページ目の表・セル構造から候補を抽出。原本保管、全候補・選択候補の一括追加、集計対象の複数指定、一括保存、保存後の手動修正に対応 | 候補追加だけでは未保存。「予算を保存」で一括登録する。小計との重複や原本総額は利用者が確認する |
| 最終原価見込 | 保管済みWeb実績＋手入力の残工事見込。未入力・未対応を未確定表示 | PDFの予算だけでは将来原価を自動算出しない。対応コードと実績鮮度の検証が必要 |

「履歴候補」は、保管済み請求の会社・工種・金額を集計して入力候補を出す仕組みです。AIモデルを追加学習させる機能ではありません。

## 試験で分かったこと・まだできていないこと

| 調査項目 | 確認済み | 未確認・次の判断材料 |
| --- | --- | --- |
| 通信データからの読取り | 通常のWeb画面が受信したJSONを解析し、3件の請求情報・工種・税率・査定金額が現在の画面読取りと一致 | 3件とも査定1行。複数行・8%・非課税・取消は未検証。通常版への組込みや直接通信への切替えはしていない |
| 保管済みの判定 | 画面からは保管済みと判定できる | JSONの単純なstatusだけでは保管済みを確定できない。完了・取消との関係の確認が必要 |
| 変更検知 | 通信データに更新日時がある | 査定だけの修正でも更新されるか未確認。今の通常更新は、一覧の基本情報が同じ査定変更を見逃す場合がある |
| CSVによる明細取得 | 標準CSVは81件・10列で取得済み。別形式が11種類あることを確認 | 標準CSVに査定明細はない。「査定」「連携」を含む別形式は出力完了まで確認できておらず、利用可否は未判定 |
| 速度 | 通常ページを読みながら通信データと照合する試験は成功 | API直接取得や全件取得の速度測定はしていない。高速化を保証できる段階ではない |
| 構造変更時の停止 | 現行読取りは構造・件数・金額を検証し、検出した不整合では履歴を更新しない | 全ての変更を検知できる保証はない。書込み用の永続凍結ガードは実保存への接続が未完了 |

通信データを使えば画面の表配置への依存を減らせる見込みがあります。ただし、通信形式や参照マスタの変更には引き続き検証と対応が必要です。現時点で公式請求APIを利用できると確認したものでもありません。

詳細: [通常版を変更せずに行った読取り試験](../../Digitalbuilder_GR-readonly-probe/RESULTS.md)。

## 正確さ・運用に関する個別課題

### 1. 税抜額の基準を画面・集計で統一する

- 確認済み不具合: 請求書本体は税込額を10%で逆算する一方、振分行は指定した税率で計算する。
- 例: 税込1,080円を税抜1,000円・8%で振り分けても、請求一覧側の税抜は981円になる。
- 作業: 税内訳を保持するか、振分完了時は振分税抜合計を用いる。未確定の10%逆算値は明示する。Excel出力の税率・税額の扱いは、既存の出力仕様凍結との整合を確認して決める。
- 完了条件: 10%・8%・非課税・混在税率で一覧、振分、集計の説明と金額が一致する。
- 根拠: [請求書リポジトリ](../invoice_manager/repositories.py) の `insert_invoice` / `get_summary`、[金額計算](../invoice_manager/utils/money_utils.py)。

### 2. 誤請求の除外を実績・候補にも反映する

- 確認済みの挙動: 新着画面で除外しても、同じ請求がWebで保管済みになれば実績・候補へ取り込まれ得る。
- 注意点: 「ローカルには取り込まない正当な請求」と「誤請求」を一律に除外すると正しい実績も消える。
- 作業: 取込対象外と実績対象外を明示的に分け、誤請求の除外状態を実績・候補・予算へ共通適用する。
- 完了条件: 誤請求はどの集計にも含まれず、単なる取込対象外の正当な実績は保持できる。
- 根拠: [新着同期](../invoice_manager/services/digital_billder_sync.py) の `set_excluded`、[履歴集計](../invoice_manager/services/historical_costs.py)。

### 3. Web工種対応の入力ミスを「実績0円」と扱わない

- 確認済み不具合: 履歴同期が一度成功した後、予算行のWeb工種対応に存在未確認のコードを入力すると、実績が0円として扱われる。
- 影響: 行の最終原価見込が実際より低く見える可能性がある。未対応実績が別に存在する場合は全体見込を止める処理があるが、誤入力そのものは検出されない。
- 作業: コード存在と対応の検証状態を保持し、「確認済みで支出0」と「未検証」を区別する。
- 完了条件: 誤字・未確認コードで最終原価を確定表示しない。
- 根拠: [予算サービス](../invoice_manager/services/project_budget.py) の `build_project_forecast`、[予算画面](../invoice_manager/ui/project_budget_window.py) の `_actual_code`。

### 4. 履歴の鮮度、中止、失敗記録を整える

- 確認済み不具合: 通常更新で詳細を再取得せずキャッシュを使った場合も、詳細取得日時 `fetched_at` が更新される。
- 現行の制約: 請求一覧の金額・会社・日付等が同じまま査定だけ変更された場合、通常更新では分からず全件再検証が必要。
- 実装不足: 全件再検証の中止、最終試行の失敗記録、予算画面への実績取得日時の表示がない。
- 作業: 一覧確認日時と詳細取得日時を分離し、実績の時点を画面へ表示する。中止と進捗・失敗状態の保存を追加し、期限を過ぎた明細だけ再検証できるようにする。
- 完了条件: 古い査定が新しい取得結果に見えず、中止しても最後に成功した履歴が保たれる。
- 根拠: [Web読取サービス](../invoice_manager/services/web_invoice_reader.py)、[履歴集計サービス](../invoice_manager/services/historical_costs.py)、[予算画面](../invoice_manager/ui/project_budget_window.py)。

### 5. PDFの種類が増えた場合の確認を強くする

- 現行仕様: 指定どおり2ページ目を抽出する。コード体系は固定せずセル構造・見出しを使う。
- 今回の改善: 抽出候補をまとめて予算行へ追加し、複数行の集計対象を設定して一括保存できる。既存の手修正値を上書きしない。
- 残る制約: 親集計と明細の同時集計や原本総額との一致は利用者確認に依存する。OCR、別ページ選択、複数ページ取込は未対応。
- 一括追加の制約: 抽出候補に金額空欄が1件でもあると、その一括追加全体を止める。金額のある候補だけ選んで追加するか、空欄候補を個別に編集する必要がある。金額を推測で0円に補う処理はない。
- 作業: 帳票が増えた段階でページ選択、親子・小計区分、原本総額との照合を追加する。推測した階層は利用者が修正可能にする。
- 完了条件: 異なるコード・列配置でも候補をレビューでき、二重計上や総額不一致を保存前に発見できる。
- 操作方法: [工事予算・最終原価見込](PROJECT_BUDGET.md)。

### 6. 編集可能な試験請求でWeb保存を実装・検証する

- 未完了理由: 利用可能な請求が支払・保管済みで、編集可能な試験請求がまだない。
- 作業: 実編集画面の構造を確認し、承認済み構造を設定。対象ID・工事・会社・金額と差分を再検証し、「編集を保存」まで接続する。
- 完了条件: 10%・8%・非課税、既存振分あり、構造変更、途中失敗を検証する。構造不一致では永続凍結し、上司へ送る「次に回す」は実行しない。
- 引継ぎ: [Web振分の実装メモ](WEB_ALLOCATION_HANDOFF.md)。

## 開発状態と検証の範囲

- 通常版は `main`、UI確認用worktreeは `codex/management-ui`。採用済み管理画面は通常版へ反映済み。
- v1.0.4の機能と公開検証記録はコミット済み（`0c8c95d`、`7300458`）。今後は [AGENTS.md](../AGENTS.md) の継続許可に従い、検証済みの変更を区切りごとにコミット・プッシュする。構想や説明書だけの更新ではアプリの版番号を上げない。
- UI用ブランチにも当時の未コミット機能のコピーが含まれる。今後の取り込みは、機能ごとの変更範囲を確認する。[UI変更手順](UI_WORKFLOW.md)を参照。
- 更新機構を含むメインテスト145件成功。システムPythonでkeyring等がない場合の資格情報保存`ModuleNotFoundError`を再現し、Python/Tk/依存パッケージ/Headless browser同梱で解消。portableでは隔離台帳へ実請求1件・PDF添付2点を登録し、通常の主台帳は変更していない。
- 配布サイトは9件の署名検証テスト、型検査、ビルドが成功。大容量送信完了時の保存済みメタデータ検証・再開・欠落・不一致の回帰確認も成功した。公開サイトからv1.0.3手動更新とWindows r2を取得し、署名とハッシュ、隔離台帳の保持、バックアップ、実請求1件・PDF2点の取込と再起動後の表示を確認。内蔵の更新確認は1.0.3を最新版と判定した。匿名操作と無効化済みトークンは403、停止した1.0.1・1.0.2の取得は404となる。
- 予算画面は既定1180×820と最小980×680で操作ボタン・入力欄・グラフが画面内に収まることをTk座標で確認。表は縦横スクロールに対応。実際のWeb保存は未検証・無効。
- 実機portable検証はWin11 x64の1台のみ。別PC、ARM、企業制限環境は未検証で、全PCの動作を保証しない。

直近の調査を踏まえた開発順は、通信データの比較試験 → 金額・除外・対応コードの修正 → 履歴更新の操作性改善です。Web保存は試験請求を用意できた時点で実画面の検証へ進めます。予算一括登録は実装・検証済みで、変更前から開いているアプリには再起動後に反映されます。
