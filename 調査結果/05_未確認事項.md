# 未確認事項

## 優先度高

- 権限による画面差分
- `対応済み` または `保管済み` で表示された実データ一覧の列構成
- 各タブでボタンを押した時の追加通信

## 優先度中

- `purchases.digitalbillder.com` 側との役割分担
- MFA 設定の実際の画面フロー
- PDF 関連画面の用途

## 優先度低

- 計測タグの詳細
- エラー画面の見た目
- PWA 的な機能の有無

## メモ

- robots は閉じられている
- 初期HTMLだけでは中身が見えない
- JavaScript bundle の解析が必要
- `/applications` の実画面は取得済み
- 現時点では「担当する請求書」の検索一覧画面として確認できた
- `要対応` が空なのは、運用上すでに全請求書が対応済みである可能性が高い
- 実データ確認は `対応済み` または `保管済み` 切替後を優先する
- 実データ一覧では `確認` リンクから `/invoices/{uuid}` 詳細へ遷移する
- 詳細画面は PDF ビューア併設型で、複数タブを持つ
- `ログ` `アクション` `付箋` `予実管理` のタブ切替だけでは追加 GraphQL は発生しなかった
- `ログ` `アクション` `付箋` `予実管理` のタブ切替だけでは追加 GraphQL は発生しなかった
